{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Launch CRUD server",
			"type": "go",
			"request": "launch",
			"mode": "auto",
			"program": "${workspaceFolder}",
			"cwd": "${workspaceFolder}"
		}
	]
}
